import Link from 'next/link';
export default function DocsPage() { return <main><header><Link href="/" className="brand">OneNote Queue</Link><nav><Link href="/setup">Setup</Link></nav></header><article className="docs"><p className="eyebrow">Documentation</p><h1>Deploy OneNote Queue</h1><h2>One-click Vercel deployment</h2><ol><li>Fork this repository and import it into Vercel.</li><li>In the Vercel Marketplace, install <strong>Neon Postgres</strong> and connect it to the project. Vercel adds the database URL automatically.</li><li>Add <code>APP_ENCRYPTION_KEY</code>: run <code>openssl rand -base64 32</code> and paste the result. Set <code>APP_URL</code> to your Vercel URL or custom domain.</li><li>Deploy. Open <Link href="/setup">Setup</Link> and follow the Microsoft app steps.</li></ol><h2>Create your Microsoft Entra app</h2><ol><li>Open <a href="https://entra.microsoft.com/" target="_blank" rel="noreferrer">Microsoft Entra admin center</a>, sign in with the Microsoft account that owns the OneNote notebooks, then open <strong>Identity → Applications → App registrations → New registration</strong>.</li><li>Name it <code>OneNote Queue</code>. Choose <strong>Accounts in any organizational directory and personal Microsoft accounts</strong>. This lets it work with personal OneDrive notebooks.</li><li>Under <strong>Redirect URI</strong>, choose <strong>Web</strong> and enter <code>https://YOUR-DOMAIN/api/auth/microsoft/callback</code> exactly. For local development use <code>http://localhost:3000/api/auth/microsoft/callback</code>.</li><li>After creating it, copy the <strong>Application (client) ID</strong>.</li><li>Open <strong>Certificates &amp; secrets → Client secrets → New client secret</strong>. Copy its <strong>Value</strong> immediately; it is only shown once. Do not use the Secret ID.</li><li>Open <strong>API permissions → Add a permission → Microsoft Graph → Delegated permissions</strong>. Add <code>User.Read</code>, <code>Notes.Create</code>, <code>Notes.ReadWrite</code>, and <code>Files.ReadWrite</code>. <code>offline_access</code> is requested by the app during sign-in.</li><li>Return to OneNote Queue Setup and paste the client ID and secret. The app encrypts the secret in your database. Click Connect Microsoft, then choose your default notebook section.</li></ol><h2>Privacy and setup progress</h2><p>The wizard saves only non-sensitive completion markers in <code>localStorage</code>, so a visitor can resume the visual setup steps in that same browser. It does not store access tokens, refresh tokens, Microsoft client secrets, API keys, note contents, or files in cookies/cache. Those must be stored encrypted in the deployment&apos;s database for the integration to work securely.</p><h2>Capture API and Shortcuts</h2><p>Create an API key after setup, then send:</p><pre>{`POST /api/capture\nAuthorization: Bearer oq_...\nContent-Type: application/json\n\n{"title":"Article","content":"My note","url":"https://example.com"}`}</pre><p>Your existing Shortcut links that are useful to document here are: Capture to OneNote, Append to OneNote, Share Sheet to OneNote, and Quick Inbox. Send their iCloud links when ready; each must send the API key in an Authorization header, never as a URL parameter.</p><h2>Advanced: Discord</h2><p>The Dashboard will include an advanced Discord setup. Create an app in the <a href="https://discord.com/developers/applications" target="_blank" rel="noreferrer">Discord Developer Portal</a>, copy its Application ID and Public Key, and set the Interactions Endpoint URL to <code>https://YOUR-DOMAIN/api/discord/interactions</code>. A Vercel deployment can receive signed Discord Interactions without a permanent bot process. A Bot Token is optional and only needed for the Docker/self-host gateway-bot mode. Never expose a bot token in a browser, Shortcut, or Git repository.</p><p>Discord commands will be registered only after the advanced setup validates the Public Key and optional test Guild ID. The first supported flow will issue a one-time link code in Discord, then have the person sign in to their own Microsoft account in the browser.</p></article></main>; }
