server {
    listen 80;
    listen [::]:80;
    server_name onenotequeue.erinskidds.com;

    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 200 "OneNote Queue certificate setup in progress\n"; }
}
