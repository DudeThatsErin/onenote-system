server {
    listen 80;
    listen [::]:80;
    server_name onenotesystem.erinskidds.com;

    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 200 "OneNote System certificate setup in progress\n"; }
}
